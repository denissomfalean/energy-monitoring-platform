spring:
  application:
    name: device-management-service
  datasource:
    url: jdbc:mysql://localhost:3306/emp-device-management-db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    user: root
    password: root
    url: jdbc:mysql://localhost:3306/emp-device-management-db
    locations: classpath:db/migration
server:
  port: 8091

jwt:
  secret: temporary-jwt-secret